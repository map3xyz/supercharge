mutation CreateBinanceOrder(
  $assetId: String!
  $userId: String!
  $orderAmount: String!
) {
  createBinanceOrder(
    assetId: $assetId
    userId: $userId
    orderAmount: $orderAmount
  ) {
    checkoutUrl
    id
    qrContent
    universalUrl
  }
}
